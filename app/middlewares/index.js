const slackRequestVerification = require('./slack_request_verification');
const slackUrlVerification = require('./slack_url_verification');

module.exports = {
    slackRequestVerification: slackRequestVerification,
    slackUrlVerification: slackUrlVerification
}